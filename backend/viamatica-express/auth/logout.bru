meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "username": "Macluiggy1",
    "password": "E123456$"
  }
}
